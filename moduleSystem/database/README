
接口使用说明：
1.数据库初始化，创建基础库
 application 入口处增加：
 DatabaseManager.get().initBaseDB(this);
  创建初始化数据库  smartshell.db

2. 根据指定string   创建数据库
DatabaseManager.get().initUserDB(Context context, String userDbName)

3.获取当前用户信息
CurrentUserManager.get().getCurrentUser()；

4.获取当前module信息
ModuleManager.get.getCurrentModule();

5.关于数据库升级 保存数据方案：
MyOpenHelper 类的方法onUpgrade中添加：
如下信息，
MigrationHelper.getInstance().migrate(db,CurrentUserDBDao.class);



6.数据库加密 没有添加。
















